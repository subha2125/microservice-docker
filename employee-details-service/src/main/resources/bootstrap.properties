spring.cloud.config.enabled = true
eureka.instance.nonSecurePort = ${server.port:8181}
eureka.client.serviceUrl.defaultZone = http://${eureka.host:localhost}:${eureka.port:8761}/eureka/

#Config server
spring.cloud.config.uri = http://${config.host:localhost}:${config.port:8888}/
spring.cloud.config.profile=development
